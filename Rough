import React from "react";
import { languages } from "../../Data/Data";
import Editor from "@monaco-editor/react";

function CodeEditor() {
  return (
    <div className="h-full w-full  flex gap-2 p-">
      <div className="h-full w-1/6 overflow-hidden  p-2">

        <div className="h-full w-full bg-white rounded-b-lg overflow-scroll gap-2 flex flex-col p-2">
          {languages.map((lang, index) => (
            <div className="p-3 bg-white rounded-md h-fit w-full cursor-pointer hover:bg-black transition-transform hover:text-white">
              {lang}
            </div>
          ))}
        </div>
      </div>

      <div className="h-full w-3/6  p-2 overflow-hidden">
        <div className="h-[5vh] w-full bg-white/30 rounded-md flex items-center p-2 gap-2"></div>

        <div className=" h-full w-full rounded-lg overflow-hidden">
          <Editor
            height="100%"
            defaultLanguage="javascript"
            defaultValue="// some comment"
            theme="vs-dark"
            options={{
              wordWrap: "on",
              minimap: { enabled: false },
            }}
          />
        </div>
      </div>
      <div className="h-full w-2/6 bg-red-300"></div>
    </div>
  );
}

export default CodeEditor;
